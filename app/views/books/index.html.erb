<h1>Här är alla dina böcker</h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book %>
      <p><%= truncate(book.body, length: 100) %></p>
      
    </li>
  <% end %>
</ul>

<%= link_to "Lägg till ny bok", new_book_path %>